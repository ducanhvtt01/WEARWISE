package com.example.dacs3.ui.theme

import androidx.compose.ui.graphics.Color

// --- LIGHT MODE COLORS ---
val OffWhite = Color(0xFFFBFBFC)
val MidnightBlue = Color(0xFF1A237E)
val SilverMist = Color(0xFF5A6A85)
val LightGray = Color(0xFFF0F2F5)
val AccentTeal = Color(0xFF00796B)
val SoftTeal = Color(0xFFE0F2F1)
val SoftOrange = Color(0xFFFFF3E0)

// --- DARK MODE ---
val DarkBackground = Color(0xFF0B0E14)      // Đen xanh đậm (giống giao diện Discord/VSC)
val DarkSurface = Color(0xFF161B22)         // Xám xanh cho các Card, nổi bật trên nền
val DarkSurfaceVariant = Color(0xFF21262D)  // Dùng cho viền (Border) hoặc Input field

// Typography (Hạn chế dùng trắng tinh để tránh mỏi mắt)
val TextDarkPrimary = Color(0xFFF0F6FC)     // Trắng nhẹ, rất sang
val TextDarkSecondary = Color(0xFF8B949E)   // Xám xanh trung tính

val DarkAccentTeal = Color(0xFF58A6FF)      // Đổi sang Blue-Teal (giống GitHub) trông công nghệ hơn
val DarkSoftTeal = Color(0xFF0D1117)        // Màu nền phụ cực tối
val DarkSoftOrange = Color(0xFF382A16)      // Cam đất trầm (chỉ dùng làm điểm nhấn rất nhỏ)